const Idea = ({ title, description }) => {
  return (
    <div>
      <img src="/" alt="pic" />
      <h1>{title}</h1>
      <p>{description}</p>
      <button>Click me</button>
    </div>
  );
};

export default Idea;
